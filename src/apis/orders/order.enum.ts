export enum OrderStatusEnum {
    Pending,
    InProgress,
    Paid,
    Cancelled
}

export enum OrderTypeEnum {
    DineIn,
    Takeaway,
    Delivery
}

export enum OrderItemStatusEnum {
    Pending,
    Preparing,
    Ready,
    Served,
    Cancelled
}